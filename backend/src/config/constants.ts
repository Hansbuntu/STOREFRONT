export const PLATFORM_FEE_PERCENT =
  Number(process.env.PLATFORM_FEE_PERCENT) || 5;

export const BUYER_CONFIRMATION_DAYS =
  Number(process.env.BUYER_CONFIRMATION_DAYS) || 7;

export const AUTO_RELEASE_DAYS =
  Number(process.env.AUTO_RELEASE_DAYS) || 14;

export const DISPUTE_WINDOW_DAYS =
  Number(process.env.DISPUTE_WINDOW_DAYS) || 21;

export const MANUAL_ESCALATION_THRESHOLD =
  Number(process.env.MANUAL_ESCALATION_THRESHOLD) || 0.4;


